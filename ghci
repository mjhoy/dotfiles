let ghciEscapeChar arg = "'" ++ concatMap (\c -> if c == '\'' then "'\"'\"'" else [c]) arg ++ "'"
:set prompt "ghci> "
:set -Wall -fno-warn-type-defaults
:def hoogle return . (":!hoogle --color " ++) . ghciEscapeChar
:def doc return . (":!hoogle --info " ++) . ghciEscapeChar
